#!/bin/bash
    echo "<-- Docker Version -->"
    docker version
    echo "<-- Docker Info -->"
    docker info
    echo "<-- Docker ps -->"
    docker ps
    echo "<-- Docker ps -a -->"
    docker image ls
    echo "<-- Docker image ls -->"
    docker volume ls
    echo "<-- Docker volume ls -->"
